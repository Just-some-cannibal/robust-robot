#pragma once

#include <iostream>
#include <fstream>
#include <chrono>
#include <string>
#include <chrono>
#include <iomanip>
#include <ctime>

namespace Robust {
  extern void log (std::string log);
  extern void err (std::string log);
}